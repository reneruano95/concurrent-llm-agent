[project]
name = "concurrent-llm-demo"
version = "0.1.0"
description = "Run concurrent LLMs on a local llama-server with a live dashboard"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32.5",
    "rich>=13.0.0",
    "openai>=2.30.0",
]
